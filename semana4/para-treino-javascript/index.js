/* 
const arrayNumbers = [1, 4, 8, 2, 5, 4, 10, 1, 2, 4, 7, 5] ;

const numeroEscolhido = prompt("Informe o número para pesquisar no array: ");

function contaOcorrencias(arrayDeNumeros, numeroEscolhido) {
    let quantidade = 0;
    
    for(i=0; i < arrayDeNumeros.length; i++){
            console.log(arrayDeNumeros[i])
            console.log(numeroEscolhido)
        if(arrayDeNumeros[i] === numeroEscolhido){
           console.log("pq vc nao entra aqui?")
        } 
        // return console.log(`O numero ${numeroEscolhido} aparece ${quantidade}x`)
    }
   return
    console.log(numeroEscolhido, quantidade)

}

contaOcorrencias(arrayNumbers, numeroEscolhido); */